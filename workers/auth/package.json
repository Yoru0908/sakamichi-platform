{
  "name": "sakamichi-auth-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 execute sakamichi-auth --file=./src/db/schema.sql",
    "db:migrate:miguri": "wrangler d1 execute miguri --file=./src/db/migrations/005_miguri.sql",
    "db:migrate:miguri-calendar": "wrangler d1 execute miguri --file=./src/db/migrations/009_miguri_calendar_subscriptions.sql"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "wrangler": "^3.60.0"
  }
}
